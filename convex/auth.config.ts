const authConfig = {
    providers: [
        {
            domain: "https://helped-boxer-29.clerk.accounts.dev/",
            applicationID: "convex",
        },
    ]
};

export default authConfig;
